%h6 Add a link
= form_for(Link.new(:topic_id => topic.id), :remote => true, html: {class: "form-inline well"} )   do |f|
	= f.hidden_field :topic_id
	= f.text_field :name, placeholder: "Name"
	= f.text_field :path, placeholder: "Link"
	= f.submit "Submit", class: "btn"